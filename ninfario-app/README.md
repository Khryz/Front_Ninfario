# React + Vite

Para correr este proyecto, ejecuta la siguiente instruccion:
npm install -> Para descargar dependencias

npm run dev -> Ejecuta el proyecto

(Recomendable que antes esté arriba el backend, sino no muestra informacion en el carrusel)
